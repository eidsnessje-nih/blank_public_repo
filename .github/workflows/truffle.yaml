on:
  push:
    branches:
    - main

jobs:
  sscan:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout code
      uses: actions/checkout@latest

    - name: TruffleHog OSS
      uses: trufflesecurity/trufflehog@v3.95.5

